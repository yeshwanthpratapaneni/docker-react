language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t ypratapa/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ypratapa/docker-react npm run test  

